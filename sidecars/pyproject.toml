[project]
name = "sidecars"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "litellm>=1.87.1",
    "pillow>=12.2.0",
    "pywin32>=312",
]

[dependency-groups]
dev = [
    "pyinstaller>=6.20.0",
    "python-dotenv>=1.2.2",
]
